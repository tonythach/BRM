<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="900">
    
    <Border Padding="30">
        <StackPanel>
            <!-- Buttons -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CityButton"  Margin="10 0 10 0" Grid.Column="0" Content="City"/>
                <Button Grid.Column="1" Content="Average Vlaue"/>
                <Button Margin="10 0 10 0" Grid.Column="2" Content="Smallest Value"/>
                <Button Grid.Column="3" Content="Largest Value"/>

            </Grid>

            <TextBlock Text="U.S. Cities" FontWeight="Bold" Margin="10 20"/>
            
            <TextBlock Text="City" Margin="10"/>
            <ComboBox Margin="10 0 550 0" Padding="2" />

            
        </StackPanel>
    </Border>
</Window>
